// Detect Cycle in Undirected Graph
// Problem - Check if an undirected graph contains a cycle.

// What it means
// Find if there's any circular path in the undirected graph.

// Constraints
// - Vertices: 1 <= V <= 1000
// - Edges: 0 <= E <= V * (V - 1) / 2
// - Time complexity: O(V + E)

// Example
// Input: V = 3, edges = [[0,1], [1,2], [2,0]]
// Output: true (cycle exists)

// Test cases
// V = 3, edges = [[0,1], [1,2]] -> false
// V = 3, edges = [[0,1], [1,2], [2,0]] -> true

